<%= form_for @o_single, :html => { :class => 'form-horizontal' } do |f| %>
  <fieldset>
    <legend></legend>
 
    <div class="control-group">
      <%= f.label :work_type, :class => 'control-label' %><span class="redStart">*</span>
      <div class="controls">
        <%= f.text_field :work_type, :class => 'text_field' %>
       	<% if @o_single.errors[:work_type].any? %>
        	<span class="text-error text-message"><%= @o_single.errors[:work_type].first %></span>
      	<% end %> 
      </div>
    </div>
    
    <div class="control-group">
      <%= f.label :fee, :class => 'control-label' %>
      <div class="controls">
        <%= f.text_field :fee, :class => 'text_field' %>
      </div>
    </div>    
 
    <div class="control-group">
      <%= f.label :is_active, :class => 'control-label' %>
      <div class="controls">
        <%= f.check_box :is_active, :class => 'check_box' %>
      </div>
    </div> 
 
    <div class="form-actions">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to 'Cancel', charges_url, :class => 'btn' %>
    </div>
    
  </fieldset>
     
<% end %>
<%- model_class = GranteeGrafe -%>
<%= hidden_field_tag :direction, params[:direction] %>
<%= hidden_field_tag :sort, params[:sort] %>
    
<div class="page-header">
  <h4><%= t("general.purchase_graves") %> (<span id="count_records"><%= @o_all.count %></span>)&nbsp;&nbsp;&nbsp;<%= session[:grantee_id] ? @grantee.name : "" %></h4>
</div>

<div class="searchRight">
	<%#= link_to 'Search', show_search_box_path(true, model_class, @search_fields.join(',')), :remote => true, :class => 'btn btn-primary btn-small' %>&nbsp;
	
	<%= link_to t('.new', :default => t("helpers.links.new")),
	            new_grantee_grafe_path,
	            :class => 'btn btn-primary btn-small' %>
	<%= link_to t('general.back'),
	            grantees_path,
	            :class => 'btn btn-primary btn-small' %>	            
</div>	


<div id="div_update_search"></div>

<div id="div_update">
	<%= render "fetchAll" %>
</div>